# Machine Reference Data

This directory contains generated controlled reference data for machine consumers, including the glossary, namespaces, source registry, relationships, cautions, and open questions.

Reference records provide the shared identifiers used by the LLM corpus, retrieval chunks, and atomic claims. They must retain human-readable labels while exposing stable machine keys and explicit cross-references.
