# LLM Corpus

This directory is reserved for the generated `llm-corpus.md`: a deterministic, ordered representation of the complete documentation for direct ingestion into a large language model context.

The corpus must preserve page titles, section boundaries, source paths, evidence qualifiers, namespace boundaries, cautions, and unresolved questions. It is generated from authoritative repository Markdown and must not be edited by hand.
