# Overview

Energy Management uses `WHO` `18` for energy measurements, totals, actuator information, and related energy-management functions.

## Reference scope

This section will document the `WHERE` classes, `DIMENSION` identifiers, value layouts, measurement and totalizer operations, actuator information, and other system-specific frame forms defined for `WHO` `18`.

See [`../../protocol/dimensions.md`](../../protocol/dimensions.md) for common `DIMENSION` frame structure.