# Overview

Dry-contact and IR functions are carried within the `WHO 25` namespace. They are functionally distinct from CEN+ while sharing the same canonical `WHO` protocol context.

Detailed `WHAT`, `WHERE`, and parameter tables will be added as the `WHO 25` reference is developed.