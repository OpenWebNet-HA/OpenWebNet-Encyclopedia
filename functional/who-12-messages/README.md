# Overview

The MyHOME_Suite `OPEN.db` system definitions identify `WHO 12` as Messages.

The current corpus establishes the functional namespace but does not yet support a complete `WHAT`, `WHERE`, or `DIMENSION` reference. Undocumented numeric values are therefore left unspecified.