# Overview

`WHO 19` is identified in the MyHOME Suite OpenWebNet system catalogue as Interface.

The current corpus establishes the namespace but does not yet provide enough supported functional semantics for a complete `WHAT`, `WHERE`, or `DIMENSION` table. Those fields remain unspecified until supported by canonical implementation data or protocol evidence.