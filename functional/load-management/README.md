# Overview

Load Management uses `WHO` `3` for load-management functions exposed through OpenWebNet.

## Reference scope

This section will document the `WHAT` values, `WHERE` grammar, `DIMENSION` identifiers, parameters, states, and system-specific frame forms defined for `WHO` `3`.

See [`../../protocol/`](../../protocol/) for common OpenWebNet frame syntax.