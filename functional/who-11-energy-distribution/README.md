# Overview

The MyHOME_Suite `OPEN.db` system definitions identify `WHO 11` as Energy distribution.

The current corpus establishes this namespace but does not yet establish a complete system-specific `WHAT`, `WHERE`, or `DIMENSION` table. It remains distinct from [`WHO 3`](../who-3-load-management/) Load Management and [`WHO 18`](../who-18-energy-management/) Energy Management.