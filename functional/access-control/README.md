# Overview

Access Control uses `WHO` `23` for access-control functions represented by OpenWebNet.

## Reference scope

This section will document access-control `WHAT` values, addressing, parameters, states, and system-specific frame forms for `WHO` `23` as supported by the available corpus.

See [`../../protocol/`](../../protocol/) for common OpenWebNet frame syntax.