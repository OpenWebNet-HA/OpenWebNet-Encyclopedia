# Overview

Video Door Entry functions span multiple OpenWebNet systems, including `WHO` `6`, `7`, and `8`.

## Reference scope

This section groups the related video-door-entry and video/multimedia protocol families while preserving the distinct semantics of each `WHO`. It will document their `WHAT` values, addressing, parameters, and system-specific frame forms separately where required.

See [`../../protocol/`](../../protocol/) for common OpenWebNet frame syntax.