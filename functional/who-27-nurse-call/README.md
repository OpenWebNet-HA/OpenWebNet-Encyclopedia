# Overview

`WHO 27` is identified in the MyHOME Suite OpenWebNet system catalogue as Nurse Call basic level.

The current corpus establishes the namespace but does not yet provide a complete supported `WHAT`, `WHERE`, or `DIMENSION` vocabulary. Unsupported semantics are left unspecified.