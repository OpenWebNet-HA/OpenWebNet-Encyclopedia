# Overview

`WHO 10` is identified in the MyHOME Suite OpenWebNet system catalogue as Navigation commands.

The current corpus establishes the namespace but the functional material assembled for this reference does not yet establish a complete `WHAT`, `WHERE`, or `DIMENSION` vocabulary for `WHO 10`. Values not supported by the canonical implementation data or a published specification are left unspecified.