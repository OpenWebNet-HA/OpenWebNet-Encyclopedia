# Overview

`WHO 3` defines the OpenWebNet Load Management system. Its commands and state information concern managed electrical loads and load-management functions.

See the [functional overview](../) and [`../../protocol/`](../../protocol/) for common frame syntax.