# Overview

Sound-system functions are represented by OpenWebNet systems including `WHO` `16` and `22`.

## Reference scope

This section will document the command, status, addressing, and `DIMENSION` forms of the sound-system protocol families while preserving the distinct grammar and semantics of each `WHO`.

See [`../../protocol/`](../../protocol/) for common OpenWebNet frame syntax.