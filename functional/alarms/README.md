# Overview

Alarms use `WHO` `5` for burglar-alarm and related alarm-system information exposed through OpenWebNet.

## Reference scope

This section will document the published `WHAT` values, `WHERE` grammar, events, states, parameters, and other system-specific frame forms defined for `WHO` `5`.

See [`../../protocol/`](../../protocol/) for common OpenWebNet frame syntax.