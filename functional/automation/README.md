# Overview

Automation uses `WHO` `2` for shutter, blind, and related automation commands, states, and `DIMENSION` operations.

## Reference scope

This section will document automation `WHAT` values, `WHERE` addressing, movement and level operations, `DIMENSION` identifiers, and system-specific frame forms.

See [`../../protocol/`](../../protocol/) for common OpenWebNet frame syntax.