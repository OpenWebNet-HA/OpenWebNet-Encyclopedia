# Overview

`WHO 6` defines the Basic Video Door Entry protocol system.

It is one of several `WHO` systems associated with Video Door Entry and multimedia functions. See the [functional overview](../) for the function-oriented grouping and [`../../protocol/`](../../protocol/) for common frame syntax.