# Overview

Lighting uses `WHO` `1` for lighting commands, states, and `DIMENSION` operations.

## Reference scope

This section will document lighting `WHAT` values, `WHERE` addressing, level and timing operations, `DIMENSION` identifiers, and system-specific frame forms.

See [`../../protocol/`](../../protocol/) for common OpenWebNet frame syntax and addressing principles.