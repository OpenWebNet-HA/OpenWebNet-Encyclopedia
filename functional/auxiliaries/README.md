# Overview

Auxiliaries use `WHO` `9` for auxiliary functions represented by OpenWebNet.

## Reference scope

This section will document auxiliary `WHAT` values, `WHERE` addressing, states, parameters, and system-specific frame forms defined for `WHO` `9`.

See [`../../protocol/`](../../protocol/) for common OpenWebNet frame syntax.