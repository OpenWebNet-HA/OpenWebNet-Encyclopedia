# Overview

`WHO 4` defines the OpenWebNet Temperature Control system. It uses its own `WHERE` addressing grammar and includes temperature, setpoint, operating-mode, actuator, and related `DIMENSION` operations.

See the [functional overview](../) and [`../../protocol/addressing.md`](../../protocol/addressing.md) for the system-scoped addressing model.