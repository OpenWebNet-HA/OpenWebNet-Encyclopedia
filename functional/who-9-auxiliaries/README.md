# Overview

`WHO 9` defines the OpenWebNet Auxiliaries system.

Its `WHAT`, `WHERE`, and any `DIMENSION` semantics are interpreted within the `WHO 9` namespace. See the [functional overview](../) and [`../../protocol/`](../../protocol/).