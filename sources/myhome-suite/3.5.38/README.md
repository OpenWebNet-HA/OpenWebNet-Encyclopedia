# MyHOME Suite 3.5.38

This source set contains canonical implementation artifacts copied from MyHOME Suite 3.5.38.

## Contents

| Directory | Contents |
| --- | --- |
| [`databases/`](databases/) | `MHCatalogue.db`, `OPEN.db`, both `ScenarioDevices.sqlite` variants, and `rules.db3` |
| [`support/`](support/) | Non-database support material, including `OpenQuery.txt` |

The original installation paths, byte sizes, SHA-256 fingerprints, and installer provenance are recorded in [`../../manifest.yaml`](../../manifest.yaml).

## Provenance

The source files were copied from a MyHOME Suite 3.5.38 installation and are retained as implementation evidence. The installer itself is not redistributed; its identifying metadata and cryptographic fingerprint are recorded in the manifest.


Canonical files in this directory must not be modified to incorporate inferred relationships, normalized data, or protocol interpretations.